import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 40,
    display: 'flex',
    justifyContent: 'flex-end',
    alignItems: 'center',
    paddingBottom: 6,

    borderBottomWidth: 1,
    elevation: 1,
  },
  text: {
    fontWeight: '500',
  },
});
